
# ── NVIDIA GPU acceleration (Linux + CUDA) ──────────────────
# tensorflow[and-cuda] bundles the CUDA/cuDNN deps via pip (TF 2.13+)
# Requires NVIDIA driver >= 525 on the host; no separate CUDA toolkit needed
tensorflow[and-cuda]>=2.13

# ── Experiment tracking & hyperparameter optimisation ───────
mlflow
optuna

# ── ML / data ────────────────────────────────────────────────
scikit-learn
pandas
numpy

# ── Visualisation ────────────────────────────────────────────
matplotlib
seaborn

# ── Notebook & Optuna progress bar ───────────────────────────
jupyterlab
ipywidgets